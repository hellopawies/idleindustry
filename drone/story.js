// Story mode controller — add UI and chapter logic here when implementing story mode.
// Reads from CHAPTERS (chapters.js) and manipulates G (farm.js).
//
// Suggested functions to implement:
//   setMode(mode)       — switch between 'sandbox' and 'story'
//   loadChapter(idx)    — reset farm + load chapter starter code
//   checkObjective()    — poll chapter.check(G) and fire chapterComplete()
//   chapterComplete()   — show completion UI, save progress to localStorage
//   renderStory()       — update the story side panel
